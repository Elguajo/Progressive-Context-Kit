# Security

Do not publish secrets/private repository content. Token optimization must never remove authorization,
trust-boundary validation, secret handling, destructive-operation approvals, data integrity, or relevant
negative tests. Applications built with this framework still need their own threat model.
